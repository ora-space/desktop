# Session Setup

Unified inputs for every ACP `session/new` and `session/load`. The first field is MCP; future
session inputs such as hooks would become additional typed fields here.

See [Session MCP](../../../../docs/session-mcp.md).

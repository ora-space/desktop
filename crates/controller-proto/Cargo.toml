[package]
name = "ora-controller-proto"
version.workspace = true
edition.workspace = true

[lints]
workspace = true

[dependencies]
prost.workspace = true
prost-types.workspace = true
tonic.workspace = true
tonic-prost.workspace = true

[features]
# Compiles the generated server modules so tests can host a fake Cloud; production never enables it.
test-server = []

/**
 * The plugin-ized trace dashboard; its workbench surface replaces the legacy iframe.
 *
 * The id is the host's canonical `<namespace>/<name>` spelling (as carried by the
 * installed-plugins snapshot), not the manifest's bare identifier.
 */
export const TRACE_DASHBOARD_PLUGIN_ID =
  "official/ora-space.agent-trace-visualizer";

# Make plugin data removal explicit

Uninstall asks whether Ora should also delete the plugin's configuration data and credentials, with deletion selected by default. Removing code must not silently choose the independent lifecycle of user data, while the secure default avoids leaving credentials behind after an ordinary uninstall.

version: "3"

tasks:
  default:
    cmds:
      - task: list

  install:
    desc: Install all dependencies for the project
    cmds:
      - pnpm install
      - cargo fetch
      - task: build

  list:
    desc: List all tasks in the current project
    cmds:
      - task --list

  lint:
    desc: Run linters on all crates
    silent: true
    cmds:
      - cargo clippy --workspace -- -D warnings

  build:
    desc: Build all crates
    cmds:
      - cargo build --workspace

  export-contracts:
    desc: Regenerate TypeScript contracts and the frontend SDK package
    cmds:
      - cargo xtask export-contracts

  run:web-frontend:
    desc: Run the web server frontend in development mode
    cmds:
      - pnpm dev

  run:web-backend:
    desc: Run the web server backend in development mode
    dir: ./.data
    env:
      ORA_PROJECT_NAME: desktop
      ORA_PROJECT_PATH: "{{.USER_WORKING_DIR}}"
    cmds:
      - mkdir -p ../.data
      - cargo run --manifest-path ../Cargo.toml --bin ora_web_server

  test:
    desc: Run contract package tests and Rust tests
    silent: true
    cmds:
      - task: lint
      - task: export-contracts
      - pnpm --filter @ora/contracts test
      - cargo test --workspace

  clean:
    desc: Clean build artifacts
    cmds:
      - rm -rf node_modules
      - |
        {{if eq OS "windows"}}
        pnpm -r exec -- powershell -NoProfile -Command "if (Test-Path node_modules) { Remove-Item -Recurse -Force node_modules }"
        {{else}}
        pnpm -r exec -- rm -rf node_modules
        {{end}}
      - pnpm store prune
      - cargo clean
